1. [HIGH] src-tauri/src/commands/file_io.rs:8 — Renderer-supplied paths are passed straight into read/write/delete/create/rename primitives, so any XSS or IPC abuse can read, overwrite, move, or delete arbitrary local files outside the workspace.
2. [HIGH] src-tauri/src/commands/templates.rs:193 — Template names are joined into `~/.gutter/templates` without sanitization, so `../` segments let reads/writes/deletes escape the templates directory and touch arbitrary user files.
3. [HIGH] src-tauri/src/commands/file_io.rs:138 — On Windows, `open_url` builds `cmd /c start <url>` from raw input, which allows shell metacharacters in a crafted URL to trigger command injection.
4. [HIGH] src/components/Editor/extensions/MermaidBlock.tsx:10 — Mermaid runs with `securityLevel: "loose"` and the returned SVG is injected with `dangerouslySetInnerHTML`, enabling stored XSS from note content.
5. [HIGH] src/components/ExportDialog.tsx:10 — HTML export/copy is built by regex substitutions on raw markdown with no escaping, so embedded HTML or quote-breaking link/image text becomes active scriptable HTML in the exported file/clipboard.
6. [MEDIUM] src-tauri/tauri.conf.json:24 — `assetProtocol.scope` is `["**"]`, which grants the webview blanket access to local files via `asset:` URLs instead of constraining loads to trusted app/workspace paths.
7. [MEDIUM] src-tauri/Cargo.toml:22 — The production Tauri build includes the `devtools` feature, making the shipped app inspectable and dramatically widening the impact of the unrestricted invoke surface.
8. [MEDIUM] src/components/FileTree/FileTree.tsx:147 — New names are concatenated directly into paths and rename only swaps the last segment, so entering `/`, `\\`, or `..` lets users create/move items outside the intended folder.
9. [MEDIUM] src/hooks/useTabLifecycle.ts:287 — Wiki/internal link handlers build target paths from note content without normalization or workspace checks, so clicking crafted links can open or create files outside the workspace root.
10. [MEDIUM] src/components/Editor/markdown/serializer.ts:101 — Node-level comments on math/mermaid/inline-math atoms are never serialized, so their anchors disappear after save/reload and the threads come back as unanchored.
11. [MEDIUM] src/hooks/useComments.ts:16 — A malformed `.comments.json` is silently treated as “no comments”, so the next save overwrites existing discussion data instead of surfacing corruption.
12. [MEDIUM] src-tauri/src/commands/history.rs:41 — Local snapshot history is keyed only by the file path hash, so renaming or moving a note orphanes its prior local history with no migration path.
13. [MEDIUM] src-tauri/src/commands/history.rs:108 — The snapshot system drops every save made within 30 seconds of the previous snapshot even when content changed, so legitimate intermediate versions are silently lost.
14. [MEDIUM] src-tauri/src/commands/file_io.rs:55 — The cross-device rename fallback recursively copies into the destination and then deletes the source, which can silently merge/overwrite existing targets instead of failing safely.
15. [MEDIUM] src-tauri/src/commands/watcher.rs:77 — Remove events only emit `tree-changed`, so open tabs never enter a deleted/conflict state and a later save silently recreates the file the user thought was gone.
16. [LOW] src/hooks/useFileWatcher.ts:22 — Single-file watching breaks on Windows paths because parent extraction only searches for `/`, so external-change detection fails when no workspace folder is open.
17. [LOW] src-tauri/src/commands/workspace.rs:19 — Directory recursion is hard-capped at depth 10, so deeper workspace content simply disappears from the file tree with no warning.
18. [LOW] src-tauri/src/commands/search.rs:54 — Workspace search also stops at depth 10, so notes in deeper folder hierarchies are never indexed or returned.
19. [LOW] src/stores/tagStore.ts:64 — Tag scanning ignores `.markdown` files even though the rest of the app opens them, so the tag browser/index is incomplete for supported documents.
20. [LOW] src/stores/backlinkStore.ts:33 — Backlink scanning only includes `.md` files, so supported `.markdown` notes never contribute backlinks.